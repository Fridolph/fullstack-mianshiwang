# 03 - 简历分析与流式押题排障记录

## 文档目的

这份文档记录本次 `apps/web-nuxt` 与 `apps/ww-server` 联调时遇到的关键故障、定位过程、修复方式和验证结果。

它重点回答 4 个问题：

- 为什么前端登录后，简历分析和流式押题会失败
- 为什么浏览器里会出现 `stream has been aborted`
- `requestId` 应该由前端生成，还是由后端补写
- 本次联调后，哪些链路已经恢复正常

---

## 一、问题现象

本次联调主要出现了 3 类问题。

### 1. 简历分析接口前端拿不到正确数据

前端调用 `POST /api/interview/analyze-resume` 后，页面虽然能收到响应，但业务层拿到的数据结构和页面预期不一致，导致 `sessionId` 与 `analysis` 取值异常。

### 2. 简历押题流式接口在前端表现为中断

前端调用 `POST /api/interview/resume/quiz/stream` 时，浏览器网络面板会看到请求建立成功，但页面会提示 `stream has been aborted`，结果页也无法稳定跳转。

### 3. 手工调试时，接口偶发 400 或保存失败

手工用 `curl` 调试流式接口时，如果没有传 `requestId`，后端会因为 DTO 校验失败直接返回 `requestId must be a UUID`。

即使补上 `requestId`，后端在接入真实 AI 生成后，也会因为题目分类和难度字段不符合 Mongo schema 的枚举要求，导致结果保存失败。

---

## 二、根因拆解

### 1. `analyze-resume` 和 `continue-conversation` 返回结构多包了一层

后端已经有统一响应结构 `{ code, message, data }`，前端请求插件也会自动拆掉最外层。

但是 `apps/ww-server/src/interview/interview.controller.ts` 之前在业务层又额外包了一层 `data`，导致前端实际拿到的是“二次嵌套”的对象。

### 2. SSE 成功路径没有发出 `complete`

`apps/ww-server/src/interview/services/interview.service.ts` 之前在成功路径里没有稳定发出 `complete` 事件，前端读流结束时只能看到连接被关闭，无法把这次流程识别成“成功结束”。

这就是浏览器里 `stream has been aborted` 的核心诱因之一。连接本身并不一定是异常断开，而是前端没有拿到一个明确的完成信号。

### 3. 前端旧连接状态清理不完整

`apps/web-nuxt/app/pages/interview/start.vue` 在重新发起流式请求时，会关闭旧连接，但旧的运行态没有在所有完成/失败分支里及时清空。

这会放大“流已经结束，但页面仍以为有活动连接”的假象。

### 4. 流式押题此前仍然使用空的占位 AI 结果

`apps/ww-server/src/interview/services/interview.service.ts` 原来在真正保存结果前，仍然使用 `const aiResult: any = {}` 这种占位写法。

这意味着即使流式接口走通，最终保存的题目列表也是空数组，前端结果页自然无法展示真实题目。

### 5. 真实 AI 输出和 Mongo schema 枚举不一致

在接入真实 AI 生成后，我复现到新的保存错误：

- AI 返回的 `category` 是 `💻 技术能力`、`🎯 项目经验`、`🧠 问题解决`
- AI 返回的 `difficulty` 是 `Easy`、`Medium`、`Hard`

但 `apps/ww-server/src/interview/schemas/interview-quiz-result.schema.ts` 期望的是：

- `category`: `technical`、`project`、`problem-solving` 等英文枚举
- `difficulty`: `easy`、`medium`、`hard`

因此 Mongoose 在保存 `ResumeQuizResult` 时直接校验失败。

### 6. `requestId` 的责任方是“发起请求的一侧”

`requestId` 不是后端在收到请求后再补写的字段，而是幂等键。

幂等键必须在请求进入服务端之前就已经存在，这样后端才能判断“这是新请求”还是“同一请求的重复提交”。

因此：

- 浏览器前端发请求时，前端生成 `requestId`
- Postman 或 `curl` 手工调试时，调用方自己生成 `requestId`
- 后端只负责校验、消费和基于它做幂等判断

当前前端已经在 `apps/web-nuxt/app/pages/interview/start.vue` 中通过 `crypto.randomUUID()` 自动生成了 `requestId`。如果是手工调试，需要自己显式传入。

---

## 三、本次修复内容

### 1. 修正简历分析和继续追问的响应结构

我在 `apps/ww-server/src/interview/interview.controller.ts` 中统一改为：

- `analyze-resume` 直接返回 `ResponseUtil.success(result, ...)`
- `continue-conversation` 直接返回 `ResponseUtil.success({ response: result }, ...)`

这样前端请求插件拆包后，页面能直接拿到单层的业务数据。

### 2. 补齐 SSE 的完成信号

我在 `apps/ww-server/src/interview/services/interview.service.ts` 中统一整理了完成事件发送逻辑：

- 缓存命中时也发送 `complete`
- 正常生成成功时发送 `complete`
- 出错时发送 `error`

前端现在可以明确区分“成功结束”和“异常中断”。

### 3. 调整前端流状态清理

我在 `apps/web-nuxt/app/pages/interview/start.vue` 中补齐了连接清理逻辑：

- `complete` 时清空 `activeConnection`
- `error` 时清空 `activeConnection`
- `onError` / `onComplete` 回调里也同步清理

这样页面不会再残留一条已经失效的旧连接状态。

### 4. 接入真实 AI 生成链路

我在 `apps/ww-server/src/interview/services/interview.service.ts` 中接入了真实的题目生成与匹配分析流程：

- 使用 `PromptTemplate`
- 使用 `StructuredOutputParser`
- 使用 `AIModelFactory`
- 分别生成题目结果和匹配分析
- 合并后保存到 `ResumeQuizResult`

这一步替换掉了原来的空占位 `aiResult`。

### 5. 增加 AI 输出归一化，解决枚举校验失败

我在 `apps/ww-server/src/interview/services/interview.service.ts` 中新增了分类和难度规范化逻辑，把 AI 常见输出映射为 schema 可接受的枚举值。

例如：

- `💻 技术能力` -> `technical`
- `🎯 项目经验` -> `project`
- `🧠 问题解决` -> `problem-solving`
- `Easy` -> `easy`
- `Medium` -> `medium`
- `Hard` -> `hard`

这样即使模型偶尔输出中文标签或带 emoji，也不会再因为枚举不一致而保存失败。

### 6. 补强 Prompt 约束

我在 `apps/ww-server/src/interview/prompts/resume-quiz.prompts.ts` 中补充了两类约束：

- 题目生成 Prompt 明确要求 `category` / `difficulty` 输出固定英文枚举
- 匹配分析 Prompt 补齐了缺失的角色说明、输入上下文和结构化输出要求

这可以减少模型偏离约定格式的概率。

### 7. 补齐保存字段和错误提示

我还补了两处容易遗漏的细节：

- 保存 `ResumeQuizResult` 时补上 `missingSkills`
- `resumeURL` 解析路径返回清理后的文本，并把最终报错改成“请提供简历内容或可访问的简历 URL”

### 8. 修复客户端断流后后台仍继续生成并命中缓存的问题

这次又补了一处更隐蔽的链路问题。

之前 `apps/ww-server/src/interview/interview.controller.ts` 在客户端断开时，只做了 RxJS `subscription.unsubscribe()`，但 `apps/ww-server/src/interview/services/interview.service.ts` 里的真实生成任务是通过 `queueMicrotask()` 启动的异步流程。

这会导致一种“看起来第一次失败、第二次却命中缓存”的现象：

- 第一次请求时，调用方或浏览器把 SSE 连接提前断开
- 前端看到的是 `abort` 或 `stream has been aborted`
- 但后端 AI 任务实际上还在继续执行
- 如果后端最终保存成功，同一个 `requestId` 第二次再发请求时，就会直接命中缓存，并返回 `isFromCache: true`

这并不表示“第一次请求真的生成失败”，而是“第一次是客户端断流，后台却没有一起取消”。

本次修复后：

- 控制器在连接关闭时，会显式通知服务层取消本次生成
- 服务层会在扣费后、创建记录后、AI 生成后、保存前后多个关键节点检查取消状态
- 如果请求已取消，会停止后续成功落库
- 若已扣次数，会自动退款
- 若已创建消费记录，会把状态更新为 `cancelled`
- 若结果文档已写入但流程尚未成功完成，会主动删除，避免后续被当成成功缓存命中

这样就能保证：**客户端断流不会再把后台任务默默跑完并写成成功结果。**

---

## 四、与 `requestId` 相关的结论

这里单独再强调一次，方便后续联调时统一口径。

### 正确做法

由请求发起方生成 `requestId`，再随请求一起发给后端。

### 为什么不能让后端事后补写

如果后端是在收到请求以后才生成 `requestId`，那它在真正处理请求前就无法知道：

- 这是第一次点击
- 还是用户重复点击
- 还是浏览器重试
- 还是网络抖动造成的重复提交

也就是说，幂等判断会失效。

### 当前项目里的实际落点

- 浏览器前端：`apps/web-nuxt/app/pages/interview/start.vue`
- 手工调试：`curl` / Postman 调用者自己传
- 后端消费记录：`apps/ww-server/src/interview/services/interview.service.ts`

### 补充说明：`sessionId` 和简历押题流不是同一个幂等键

`POST /api/interview/analyze-resume` 返回的 `sessionId`，主要用于后续“继续追问”场景。

当前 `POST /api/interview/resume/quiz/stream` 的幂等判断并不依赖 `sessionId`，而是依赖调用方传入的 `requestId`。

所以在联调时要注意：

- `analyze-resume` 成功，只说明简历分析链路正常
- `resume/quiz/stream` 是否命中缓存，取决于这次请求带的 `requestId`
- 如果要排查一次全新的押题流程，应该生成一个新的 `requestId`
- 如果要确认旧请求是否还在执行，应该查 `GET /api/interview/resume/quiz/status?requestId=...`

---

## 五、验证结果

本次我用本地联调环境做了两轮回归。

### 1. `analyze-resume` 回归

调用 `POST /api/interview/analyze-resume` 后，接口已经能稳定返回单层业务数据，包含：

- `data.sessionId`
- `data.analysis`

前端不再需要从双层 `data.data` 中取值。

### 2. `resume/quiz/stream` 回归

我使用登录后的测试账号直连 `POST /api/interview/resume/quiz/stream`，确认事件顺序如下：

- `progress` 5
- `progress` 20
- `progress` 40
- `progress` 80
- `complete` 100

最终返回结果中已经包含：

- `resultId`
- 非空 `questions`
- `summary`
- `matchScore`
- `matchedSkills`
- `missingSkills`
- `learningPriorities`
- `radarData`
- `interviewTips`

这说明当前后端已经可以完成“生成题目 -> 保存结果 -> 发出完成事件”的完整链路。

### 3. 断流取消回归

在本次补丁后，`resume/quiz/stream` 还需要满足新的回归预期：

- 如果客户端在 `complete` 前主动断开连接，本次请求不会再被记成 `success`
- 同一个 `requestId` 再次查询状态时，应看到 `cancelled`，而不是缓存成功结果
- 再次发起正式调试时，应更换新的 `requestId`
- 如果只是想看后台是否仍在处理，优先使用状态查询接口，而不是重复发同一个 `requestId`

---

## 六、这次排障涉及的主要文件

- `apps/ww-server/src/interview/interview.controller.ts`
- `apps/ww-server/src/interview/services/interview.service.ts`
- `apps/ww-server/src/interview/services/resume-analysis.service.ts`
- `apps/ww-server/src/interview/prompts/resume-quiz.prompts.ts`
- `apps/web-nuxt/app/pages/interview/start.vue`
- `apps/web-nuxt/nuxt.config.ts`
- `apps/web-nuxt/.env.example`

---

## 七、后续建议

当前主链路已经恢复，但后续仍建议继续做两件事。

### 1. 给简历押题补结果详情和回放测试

现在流式接口已经可用，但最好再补一层接口级测试或 e2e 用例，覆盖：

- 正常生成
- 重复 `requestId` 命中缓存
- AI 失败后的退款逻辑

### 2. 统一前后端的枚举展示层

后端存储现在使用英文枚举更稳定，但前端展示时可以再做一层映射，把：

- `technical` 显示为“技术能力”
- `project` 显示为“项目经验”
- `problem-solving` 显示为“问题解决”

这样既保证数据层稳定，也能保持中文界面体验。

---

## 八、Flashpost / Postman 手动调试说明

手动调试 `POST /api/interview/resume/quiz/stream` 时，最容易踩的坑不是请求体，而是 `requestId`、鉴权头和 SSE 的使用方式。

### 1. 必须带上的请求头

这条接口至少要带上下面两个头：

- `Authorization: Bearer <token>`
- `Accept: text/event-stream`

请求体仍然使用 JSON，所以你也应该保留：

- `Content-Type: application/json`

### 2. 每次手动发送都要换一个新的 `requestId`

后端把 `requestId` 作为幂等键使用。同一个用户如果重复提交同一个 `requestId`，后端会先检查这条记录的状态：

- `pending`：返回“请求正在处理中，请稍后查询结果”
- `success`：直接命中缓存结果
- `failed`：说明上次处理失败

这意味着手工调试时，你不能把同一个 body 连续发送多次，除非你就是想复用那次请求的结果。

### 3. Flashpost / Postman 的推荐流程

你可以按下面顺序调试：

1. 先登录，拿到 JWT token。
2. 构造新的 `requestId`，每次发送前都换一个新的 UUID。
3. 调 `POST /api/interview/resume/quiz/stream`，观察 SSE 返回的 `progress` / `complete` / `error`。
4. 如果中途断开，或者你怀疑还在处理中，再调用状态查询接口确认当前状态。

### 4. 新增的状态查询接口

为了方便手工联调，后端新增了一个查询接口：

- `GET /api/interview/resume/quiz/status?requestId=<uuid>`

这条接口同样需要登录态。它会返回当前 `requestId` 对应的处理状态：

- `pending`
- `success`
- `failed`

当状态是 `success` 时，响应里会直接附带对应的结果明细，包括：

- `resultId`
- `summary`
- `questions`
- `matchScore`
- `matchedSkills`
- `missingSkills`
- `learningPriorities`
- `radarData`

### 5. 一个可直接参考的调试方式

当你在 Flashpost 里手动发流式请求时，可以这样理解：

- `stream` 接口负责“发起生成”
- `status` 接口负责“查询这一单现在走到哪一步”

这种组合特别适合手工联调，因为你不需要每次都盯着一条长连接看是否结束。

---

## 6. 历史记录详情与开始页体验补充

日期：

- 2026-04-03

这次补充了两块和用户体验直接相关的链路：

- 历史记录页支持点击单条记录进入详情页
- 开始页支持 mock 快速填充和简历文件文本回填

### 6.1 历史记录详情页

新增能力：

- 点击 `/history` 中的单条服务记录，可进入 `/history/[recordId]`
- 详情页会展示：
  - 当次输入快照（公司、岗位、薪资、JD）
  - 当前处理状态
  - 已生成的结果内容
  - 若记录中保留了会话上下文，则展示历史对话
- 如果该记录仍是 `pending`，并且保留了 `sessionId`，可继续在原会话里追问，但不会重新开启新会话

实现说明：

- 后端新增 `GET /api/interview/records/:recordId`
- 前端列表页仍然走 `GET /user/consumption-records`
- 详情页再额外调用单条详情接口获取结果与会话信息

注意：

- 旧版本消费记录未必保存了 `sessionId`
- 这类旧记录依然可以看输入快照和结果，但可能没有可回放的对话内容
- 新生成的简历押题记录会在创建消费记录时同步落下分析结果与对话快照，详情页会优先读取这份持久化数据；这样即使服务热重启、内存会话被清理，历史详情也不会直接丢失原始上下文

### 6.2 开始页 mock 快填与文件读取

新增能力：

- 在 `/interview/start` 增加“一键填充 mock 信息”按钮
- 支持上传 `pdf`、`doc`、`docx`、`md` 文件
- 上传后先做文本解析，再把结果直接回填到“简历文本” textarea

实现说明：

- 前端新增上传交互，但不直接在浏览器里解析文件
- 后端新增 `POST /api/interview/resume/parse-file`
- 解析职责集中在后端的 `DocumentParserService`

这样做的好处：

- 前端只负责选择文件和回填文本，逻辑更轻
- 文档解析格式统一在后端收口，后续扩展更容易
- 当前阶段先保证“能上传、能提取、能继续走简历分析/押题链路”
